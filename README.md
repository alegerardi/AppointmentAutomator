# AppointmentAutomator
Backend-focused prototype of a WhatsApp reservation system built with Flask and Twilio. Handles stateful booking flows, service availability, date/time selection, database persistence, owner notifications, and an authenticated admin dashboard. Exposed locally for webhook testing.
